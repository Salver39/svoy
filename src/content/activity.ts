// Копи для выбора уровня активности в онбординге (фидбэк 2026-06-19: непонятно,
// что значит «иногда» / «регулярно»). Под каждым уровнем — нейтральный пример.
//
// Тон (принцип 13, F8): говорим через «активность» и повседневную нагрузку, БЕЗ
// слов тренировка/спорт/кардио/силовая/сожжено. Это сигнал об образе жизни —
// СОЗНАТЕЛЬНО не влияет на расчёт зоны (R1), поэтому формулировки описательные,
// а не «чем больше, тем выше норма».

import type { ActivitySignal } from '@/db/schema';

export const ACTIVITY_COPY: Record<ActivitySignal, { label: string; hint: string }> = {
  none: {
    label: 'Почти нет',
    hint: 'в основном сидячий день, без заметной нагрузки',
  },
  sometimes: {
    label: 'Иногда',
    hint: 'прогулки и активность несколько раз в неделю',
  },
  regular: {
    label: 'Регулярно',
    hint: 'активность почти каждый день',
  },
};

export const ACTIVITY_ORDER: ActivitySignal[] = ['none', 'sometimes', 'regular'];
