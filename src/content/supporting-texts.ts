// Тексты экрана заземления (BACKLOG E6, кнопка «тревожно» → /grounding).
//
// ✅ SAFETY-3: формулировки ниже прошли ревью специалиста (психолог + UX writer)
// 2026-06-16. Тег «SAFETY-3» на экспортах = принадлежность к ревью-набору.
//
// Правила копи (SPEC принципы 5, 6, 11):
//  - Никогда о теле / весе / еде. Только про дыхание, ощущение, момент.
//  - Gender-neutral: настоящее время, безличное, без рода. Без феминитивов.
//  - Не оценочно: без «молодец / справишься / держись». Спокойная констатация.

/** Параметры дыхания 4-7-8 (вдох-задержка-выдох), секунды. SAFETY-3. */
export const BREATH_478 = {
  inhale: 4,
  hold: 7,
  exhale: 8,
  inhaleLabel: 'вдох',
  holdLabel: 'задержка',
  exhaleLabel: 'выдох',
} as const;

/** Короткая инструкция над анимацией дыхания. SAFETY-3. */
export const GROUNDING_INTRO =
  'Несколько спокойных циклов дыхания. Круг подсказывает ритм: расширяется — вдох, замирает — задержка, сжимается — выдох.';

/** Поддерживающие фразы (показываем одну, ротация по заходам). SAFETY-3. */
export const SUPPORTING_PHRASES: readonly string[] = [
  'Сейчас можно просто побыть здесь.',
  'Внимание возвращается к дыханию.',
  'Следующий вдох уже рядом.',
];

/** Подпись внизу — мягкий выход без обязательств. SAFETY-3. */
export const GROUNDING_OUTRO = 'Можно продолжать или вернуться позже.';
