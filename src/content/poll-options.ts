// Опрос состояния для мягкого режима (BACKLOG E5 AC #3,6, SPEC «Мягкий режим»).
//
// ✅ SAFETY-3: формулировки прошли ревью специалиста (психолог + UX writer)
// 2026-06-16. Список НЕ оценочный (никаких «достаточно / хорошо / много»),
// единая безоценочная конструкция наблюдения. Gender-neutral (SPEC принцип 14).
//
// «не знаю» — первая опция всегда (под-репорт = под-репорт важнейшего сигнала).
// «другое» — открытое поле, всегда видимо. Середина рандомизируется при показе.
// MVP-cut: один набор; ротация формулировок раз в N дней деактивирована до
// SAFETY-3 (см. poll-rotation.ts).

export interface PollOptionSet {
  /** Всегда первой, не перемешивается. */
  dontKnow: string;
  /** Середина — порядок рандомизируется при каждом показе. Только нейтральное. */
  middle: string[];
  /** Открытое поле, всегда видимо последним. */
  other: string;
}

export const POLL_SETS: PollOptionSet[] = [
  {
    dontKnow: 'не знаю',
    middle: ['есть голод', 'есть сытость', 'есть тяжесть', 'спокойно'],
    other: 'другое',
  },
];

/** Показывается один раз перед первым опросом (BACKLOG E5 AC #5). SAFETY-3. */
export const POLL_INTRO = 'Подходит любой вариант. Можно выбрать «не знаю».';
